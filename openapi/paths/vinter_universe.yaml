get:
  tags:
    - Vinter Universe
  summary: Get the Vinter universe of elgible exchanges and constituents
  description: |
    This endpoint returns the current eligible constituents and exchanges.
  operationId: 
  parameters:
  security:
    - main_auth:
        - 'read:users'
    - api_key: []
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            # $ref: ../components/schemas/universe.yaml