title: non_success
type: object
x-examples:
  example:
    result: error
    message: Too Many Requests -- Rate limit exceeded
    params:
      symbol: D1H_BTCUSD_DRR_MEAN
    data: ''
properties:
  result:
    description: Takes the value error for all error messages
    type: string
    example: error
  message:
    description: Error message
    type: string
    example: Unknown error
  params:
    description: The parameters that were used in the request
    type: object
    example:
      symbol: D1H_BTCUSD_DRR_MEAN
  data:
    description: Empty string
    type: string
    example: ""
description: The schema that is returned for an errod code
